# Virmeet — תוכנית עבודה

מסמך העברה לשיחה חדשה. כתוב כך שסוכן/מפתח שלא ראה את הקוד יוכל לבצע ממנו
ישירות, בלי להסתמך על היסטוריית שיחה קודמת.

**סטטוס:** התוכנית נגזרת מסקירת קוד מלאה (46 קבצים, ~6,000 שורות) שבוצעה על
הענף `claude/app-feedback-p143xq`. שום שינוי קוד לא בוצע — כל הסעיפים כאן
פתוחים.

---

## 0. איך להשתמש במסמך הזה

1. קרא את פרק 1 (הקשר) ופרק 2 (אינווריאנטים) — הם קצרים וחוסכים טעויות יקרות.
2. בחר משימה מפרק 3. המשימות ממוינות לפי ערך/מאמץ; A1–A4 הן החסימות האמיתיות.
3. כל משימה כוללת: **הבעיה** (עם הפניה לקובץ:שורה), **מה לעשות**,
   **קריטריון קבלה**, ו**סיכונים**.
4. אין תשתית בדיקות בפרויקט. משימה B1 מקימה אותה; אם אתה נוגע ב-`runner.ts`,
   שקול לעשות אותה קודם.

הרצה מקומית:

```bash
npm install
cp .env.example .env.local     # והזן ANTHROPIC_API_KEY
npm run dev                    # http://localhost:3000
npm run typecheck              # tsc --noEmit — חייב לעבור לפני כל commit
npm run build
```

---

## 1. הקשר — מה זה Virmeet

כלי לסימולציית פגישה עם פרסונות ארגוניות. המשתמש בוחר משתתפים, מגדיר מטרת
פגישה, מעלה חומרי רקע — וסוכני LLM מקיימים דיון שמסתיים בתמליל ובמשימות.

**התזה של המוצר:** הערך הוא לא ה"תמליל" אלא **מה יישאל בפגישה האמיתית ואין עליו
תשובה**. המנגנון שמייצר את זה הוא **א-סימטריית ידע** — לכל פרסונה פרומפט משלה,
קבצים משלה, ו-KPI משלה.

### ארכיטקטורה ב-60 שניות

```
src/
  app/
    api/                 REST + SSE. כל route מוולד עם zod ומחזיר { error } בעברית
    meetings/[id]/       מסך ההרצה: SSE + fallback ל-polling
    meetings/new/        אשף פגישה חדשה
  lib/
    engine/
      runner.ts          מכונת המצבים — הלב של המערכת
      prompts.ts         בוני הפרומפטים בעברית
      schemas.ts         JSON schemas ל-structured output
      budget.ts          תקציב קריאות per-persona
    anthropic.ts         עטיפה ל-SDK: caching, retry, web search, refusal
    store.ts             אחסון JSON על הדיסק, כתיבה אטומית, נעילה per-file
    extract.ts           חילוץ טקסט מ-pdf/docx/txt
    seed.ts              4 פרסונות, 5 סוגי פגישה, הגדרות ארגון
data/                    נתוני ריצה — לא בגיט
```

### מכונת המצבים (`src/lib/engine/runner.ts`)

| שלב | מה קורה | קריאות מודל |
|---|---|---|
| `prep` | כל פרסונה קוראת את החומרים **בנפרד ובמקביל**, מייצרת הבנה + 3 חששות + 3 שאלות | N (מקבילי) |
| `opening` | המנחה מקבל את כל פלטי ההכנה ומזהה 2–4 התנגשויות | 1 |
| `discussion` | R סבבים (1–4). בכל סבב כל פרסונה מגיבה למה שנאמר בפועל | N × R (סדרתי) |
| `convergence` | המנחה מסכם: מה סוכם, מה פתוח, מי מחליט | 1 |
| `extraction` | חילוץ `MeetingResult` מובנה | 1 |

מודלים: פרסונות `claude-sonnet-5` (ניתן לשינוי per-persona), מנחה `claude-opus-5`
(קבוע ב-`MODELS.facilitator`, `src/lib/types.ts:3`).

ההרצה משודרת ב-SSE מ-`POST /api/meetings/[id]/run`. צורות האירועים:
`{type:'phase'|'entry'|'done'|'error'}`.

---

## 2. אינווריאנטים — אסור לשבור

אלה החלטות עיצוב מכוונות. שינוי שלהן מפרק את המוצר, לא רק את הקוד.

1. **שלב `prep` מבודד.** כל פרסונה מייצרת את ההכנה שלה בלי לראות אף אחד אחר
   (`runner.ts:163`). אל "תייעל" את זה להקשר משותף — זה מה שמונע עדריות.
2. **א-סימטריית ידע.** קבצי פרסונה פרטיים לפרסונה; רק `meeting.files` משותפים.
   אל תאחד אותם.
3. **יציבות ה-system blocks לאורך ריצה.** `buildPersonaSystemBlocks` מחזיר תמיד
   בדיוק את אותם בלוקים לכל אורך הפגישה — זה מה שמאפשר prompt caching. אל תכניס
   תוכן per-phase או per-round ל-system.
4. **טקסט הדיסקליימר מופיע מילה במילה** בראש כל ייצוא Markdown
   (`export/render.ts`, הקבוע `DISCLAIMER_HE`) ובראש מסך התוצאות.
5. **כל שגיאה שמוצגת למשתמש היא בעברית.** אין להדליף הודעות zod/SDK באנגלית.
6. **כשל פרסונה בודדת לא מפיל את הפגישה** — נרשמת שורת `system` בתמליל והדיון
   ממשיך.
7. **מזהי מודלים בדיוק כפי שהם** — `claude-sonnet-5`, `claude-opus-5`,
   `claude-haiku-4-5`. בלי סיומות תאריך.
8. **פרמטרי מודל:** `thinking: {type:'adaptive'}` בלבד. אין `budget_tokens`,
   אין `temperature`/`top_p`/`top_k` — כולם מחזירים 400 במודלים האלה.
   עומק חשיבה נשלט דרך `output_config.effort`.

---

## 3. המשימות

### קבוצה A — חסימות (לבצע ראשונות)

---

#### A1 · קבצי הרקע המשותפים נשלחים ללא cache בכל קריאה

**הבעיה.** `buildFilesBlock(meeting.files)` יושב בתוך `meetingHeaderBlock`
(`src/lib/engine/prompts.ts:96-109`, השורה הרלוונטית היא 108), ו-
`meetingHeaderBlock` נדחף ל-**user message** של `prep`, `discussion`,
`convergence` ו-`extraction`. ה-`cache_control` מוגדר רק על ה-system blocks
(`src/lib/anthropic.ts:90-98`).

סדר הרינדור של ה-API הוא `tools → system → messages`, ו-cache הוא התאמת prefix.
לכן היום נשמרים ב-cache רק שלושת בלוקי ה-system (org + prompt הפרסונה + קבצי
הפרסונה), וכל ה-user message נשלח בתשלום מלא בכל קריאה.

**הסדר גודל.** קובץ רקע משותף אחד ב-60K תווים (המקסימום ב-`extract.ts`) הוא
בערך 20K טוקנים. עם 4 פרסונות × (הכנה + 2 סבבים) + 3 קריאות מנחה = 15 קריאות
× 20K ≈ **300K טוקני קלט לא-cached בפגישה אחת** — רק בשביל קובץ אחד.

**מה לעשות.**

הפתרון הפחות פולשני: לפצל את ה-user message לבלוקי תוכן ולשים `cache_control`
בסוף החלק היציב. `meetingHeaderBlock` זהה בכל הקריאות של אותה פגישה, ויושב
בתחילת ההודעה — בדיוק תבנית "shared prefix, varying suffix".

1. שנה את `CallModelMessage` כך שהתוכן יוכל להיות מערך בלוקים (הטיפוס
   `Anthropic.MessageParam` כבר תומך בזה — ייתכן שלא נדרש שינוי טיפוס כלל).
2. ב-`runner.ts`, במקום `content: prompts.buildXUserMessage(...)` המחזיר מחרוזת,
   העבר שני בלוקים:

   ```ts
   content: [
     { type: 'text', text: header, cache_control: { type: 'ephemeral' } },
     { type: 'text', text: rest },
   ]
   ```

3. פצל כל `build*UserMessage` ב-`prompts.ts` לשתי פונקציות — או החזר
   `{ header, rest }` — כך ש-`header` הוא בדיוק הפלט של `meetingHeaderBlock`
   ו-`rest` הוא כל השאר (מסגור, התנגשויות, תמליל, המשימה).

זה מספיק. הפרדה יחידה זו מרחיבה את ה-prefix ה-cached לכלול את כל בלוקי ה-system
**וגם** את כותרת הפגישה עם הקבצים המשותפים.

**דגשים טכניים (מאומתים מול התיעוד).**

- מקסימום **4 נקודות `cache_control` לבקשה**. אנחנו נשתמש ב-2 (הבלוק האחרון
  ב-system + הכותרת ב-user). בסדר גמור.
- **אורך מינימלי ל-cache:** 512 טוקנים ב-`claude-opus-5`, **1024** ב-
  `claude-sonnet-5`. מתחת לזה ה-cache פשוט לא נוצר — **בלי שגיאה**, רק
  `cache_creation_input_tokens: 0`. פגישה בלי קבצים ועם פרסונה קצרה עלולה לא
  להיכנס לסף. זה תקין, לא באג.
- **TTL ברירת מחדל הוא 5 דקות.** מחיר כתיבה 1.25×, קריאה ~0.1×. נקודת האיזון
  היא שתי בקשות — כל פרסונה מבצעת לפחות 1 (prep) + R (סבבים), אז זה משתלם
  מסבב אחד ומעלה.
- אפשר `cache_control: { type: 'ephemeral', ttl: '1h' }`. **אל תעשה את זה
  בעיוור** — מחיר הכתיבה קופץ ל-2× ונקודת האיזון עולה לשלוש בקשות. תור פרסונה
  אחת יכול לקחת 30–60 שניות, ובפגישה עם 4 משתתפים המרווח בין סבבים עלול
  להתקרב ל-5 דקות. **קודם מדוד** (ראה קריטריון קבלה), ורק אם רואים החטאות
  עבור ל-`1h`.

**קריטריון קבלה.**

- הרץ פגישה עם קובץ רקע משותף אחד (≥5K תווים), 2 סבבים, 3 משתתפים.
- לוג את `usage.cache_read_input_tokens` בכל קריאה (הערך כבר נאסף ב-
  `CallModelUsage`, `src/lib/anthropic.ts:159`).
- **צפוי:** בקריאה הראשונה של כל פרסונה `cache_creation_input_tokens > 0`;
  בכל קריאה שאחריה עבור אותה פרסונה `cache_read_input_tokens > 0`.
- אם `cache_read_input_tokens` נשאר 0 — יש invalidator שקט. חפש הבדל בייטים
  ב-header בין קריאות (תאריך, מזהה, סדר מפתחות ב-JSON).

**סיכונים.** נמוך. אין שינוי בתוכן שנשלח למודל — רק בחלוקה לבלוקים. שמור על
`meetingHeaderBlock` **זהה בייט-לבייט** בין כל הקריאות של אותה פגישה.

---

#### A2 · "בטל פגישה" לא מבטל כלום

**הבעיה.** `handleCancel` (`src/app/meetings/[id]/page.tsx:446-459`) עושה שני
דברים: abort ל-fetch, ו-PATCH לסטטוס `cancelled`.

- ה-abort סוגר רק את ה-stream בצד הלקוח. `cancel()` ב-`run/route.ts` מסמן
  `closed = true` אבל `runMeeting()` ממשיך לרוץ בשרת עד הסוף — זה מכוון
  ומתועד שם.
- גרוע מכך: ה-`emitPhase` הבא קורא ל-`persist({ status: 'running' })`
  (`src/lib/engine/runner.ts:137-140`) ו**דורס את הביטול**. בסוף גם
  `persist({ status: 'completed' })`.

תוצאה: הכסף ממשיך להישרף, ומשתמש שיטען מחדש את הדף יראה "רצה כעת".

**מה לעשות.**

1. הוסף בדיקת ביטול ב-`runMeeting`, בין שלבים ובין תורות בתוך `discussion`:

   ```ts
   async function isCancelled(): Promise<boolean> {
     const current = await deps.getMeeting(meetingId);
     return current?.status === 'cancelled';
   }
   ```

   קרא לזה בראש כל שלב ובראש כל איטרציית פרסונה בלולאת הדיון. אם מבוטל —
   רשום שורת `system` בתמליל ("הפגישה בוטלה על ידי המשתמש"), שדר
   `{type:'error'}` או אירוע סיום, וצא.

2. `emitPhase` **חייב להפסיק לכתוב `status:'running'` בעיוור.** או שהוא לא נוגע
   בסטטוס בכלל (הסטטוס נקבע פעם אחת בתחילת הריצה, `runner.ts:155`), או שהוא
   כותב `running` רק אם הסטטוס הנוכחי אינו `cancelled`.

3. שקול לבדוק גם `cancelled` ב-`run/route.ts` לפני שמתחילים.

**קריטריון קבלה.** הרץ פגישה, לחץ "בטל" באמצע שלב הדיון. הריצה נעצרת תוך תור
אחד לכל היותר; אחרי רענון דף הסטטוס הוא `cancelled` ולא `running`/`completed`;
התמליל שנוצר עד הביטול נשמר; לא בוצעו קריאות מודל נוספות (בדוק את
`usage.apiCalls`).

**סיכונים.** קריאת `getMeeting` בכל תור מוסיפה I/O. זניח מול קריאת מודל.

---

#### A3 · פגישה שנכשלה היא מבוי סתום, והרצה חוזרת משכפלת את התמליל

**הבעיה.** שני באגים קשורים:

1. `runMeeting` מתחיל מ-`transcript = [...meeting.transcript]`
   (`src/lib/engine/runner.ts:122`). `run/route.ts` חוסם רק `running` ו-
   `completed`, כך שפגישה `failed` **ניתנת** להרצה חוזרת — והתמליל הישן יישאר
   ותמליל חדש יתווסף אליו.
2. ה-UI בכלל לא מציע להריץ שוב — ההרצה האוטומטית קורית רק כש-`status === 'draft'`
   (`src/app/meetings/[id]/page.tsx:393`). כלומר פגישה שנכשלה בשלב `extraction`
   היא מבוי סתום, למרות שהתמליל היקר נשמר בהצלחה — וזה בדיוק המקרה שבשבילו
   נכתבה השמירה.

**מה לעשות.**

1. **הוסף route לחילוץ מחדש:** `POST /api/meetings/[id]/extract`. הוא מריץ
   **רק** את שלב ה-extraction על התמליל הקיים, בלי לגעת בשלבים הקודמים. חלץ
   את גוש ה-extraction מ-`runner.ts:404-455` לפונקציה
   `runExtraction(meeting, participants, transcript, convergenceSummary, apiKey)`
   וקרא לה משני המקומות.
   - `convergenceSummary` לא נשמר על ה-Meeting. אפשר לשחזר אותו מהתמליל: הרשומה
     האחרונה עם `phase === 'convergence'` ו-`speakerId === 'facilitator'`.
2. **בטל את ההרצה החוזרת המשכפלת:** ב-`run/route.ts` חסום גם `status === 'failed'`
   עם הודעה מתאימה (ההפעלה מחדש עוברת דרך ה-route החדש).
3. **UI:** במסך הפגישה, כאשר `status === 'failed'` והתמליל אינו ריק — הצג כפתור
   "נסה לחלץ משימות שוב".

**קריטריון קבלה.** גרום ל-extraction להיכשל (למשל מפתח API שגוי רק לקריאה הזו,
או `max_tokens` נמוך במכוון). ודא: התמליל נשמר; הסטטוס `failed`; מופיע כפתור
חילוץ מחדש; לחיצה עליו מייצרת `MeetingResult` תקין ומעבירה ל-`completed`;
**התמליל לא הוכפל**.

**סיכונים.** בינוני — נגיעה בליבת ה-runner. עשה את B1 (בדיקות) קודם אם אפשר.

---

#### A4 · `max_tokens` של שלב החילוץ עלול לקצץ את ה-JSON

**הבעיה.** `REGULAR_MAX_TOKENS = 8000` (`src/lib/engine/runner.ts:40`) משמש
בכל חמש הקריאות, כולל `extraction`. שם הפלט הוא אובייקט מובנה גדול —
summary, decisions, openQuestions, conflicts, risks, **tasks** (עם description,
assumption ו-riskIfAssumptionWrong לכל משימה), modelAssumptions.

שתי בעיות מצטברות:

1. **טוקני החשיבה נספרים בתוך `max_tokens`.** הקריאה רצה על `claude-opus-5`
   עם `thinking: {type:'adaptive'}` ו-`effort: 'high'`, כך שחלק ניכר מ-8000
   יכול להיבלע בחשיבה לפני שנכתב תו אחד של JSON.
2. **אין בדיקת `stop_reason`.** `extractResult` (`src/lib/anthropic.ts:133`)
   בודק רק `refusal`. אם התשובה נקטעה ב-`max_tokens`, ה-JSON חלקי,
   `JSON.parse` זורק, והפגישה מסומנת `failed` — עם הודעת שגיאה מטעה על JSON
   לא תקין במקום הסיבה האמיתית.

**מה לעשות.**

1. הפרד קבוע: `EXTRACTION_MAX_TOKENS = 32000` והשאר את השאר על 8000.
   `STREAMING_THRESHOLD = 16000` (`src/lib/anthropic.ts:76`) יגרום לקריאה הזו
   לעבור אוטומטית ל-streaming — וזה הדבר הנכון: בקשה לא-streaming עם
   `max_tokens` גבוה חשופה ל-HTTP timeout.
2. הוסף `stopReason` ל-`CallModelResult` והחזר את `message.stop_reason`.
3. ב-`runner.ts`, לפני ה-`JSON.parse`, אם `stopReason === 'max_tokens'` — זרוק
   שגיאה בעברית מפורשת ("פלט החילוץ נקטע באמצע — הפגישה ארוכה מדי למגבלת
   הטוקנים הנוכחית"), לא הודעה על JSON פגום.

**קריטריון קבלה.** הרץ פגישה ארוכה (4 משתתפים, 4 סבבים, קבצי רקע). שלב החילוץ
מסתיים בהצלחה. הורד במכוון את `EXTRACTION_MAX_TOKENS` ל-500 וודא שההודעה
שמוצגת היא הודעת הקיצוץ ולא הודעת ה-JSON.

**סיכונים.** נמוך.

---

### קבוצה B — יסודות

---

#### B1 · חבילת בדיקות ראשונה ל-`runner.ts`

**הבעיה.** אין שום בדיקה בפרויקט ואין מריץ בדיקות ב-`package.json`. יחד עם זאת
`runMeeting` **כבר** בנוי להזרקת תלויות (`overrideDeps`, `runner.ts:89`),
ומנגנון שקיים אך ורק לצורך בדיקות — ואין בדיקות.

**מה לעשות.**

1. הוסף `vitest` ל-devDependencies + סקריפט `"test": "vitest run"`.
2. כתוב `src/lib/engine/runner.test.ts` עם `callModel` מזויף. כסה:
   - **המסלול המאושר** — 2 פרסונות, סבב אחד, פלט תקין בכל שלב → `completed`,
     `result` מלא, מבנה התמליל צפוי.
   - **כשל פרסונה בודדת ב-`prep`** → נרשמת שורת `system`, הפגישה ממשיכה.
   - **`refused: true`** מפרסונה → שורת `system` מתאימה, ממשיכים.
   - **JSON לא תקין מ-`prep`** → שורת `system`, הפרסונה לא נכנסת ל-`prepResults`.
   - **מיצוי תקציב** — `maxApiCalls: 1` → הפרסונה מדולגת בסבבים הבאים, הודעת
     "הגיע/ה לתקציב" מופיעה **פעם אחת בלבד** (`budget.ts`).
   - **כשל ב-`extraction`** → `status: 'failed'` **והתמליל נשמר**.
   - **פחות משני משתתפים** → אירוע `error`, אין קריאות מודל.
3. בדיקות יחידה נפרדות ל-`CallBudget` ול-`sanitizeFilename`.

**קריטריון קבלה.** `npm test` ירוק; כל הבדיקות רצות בלי גישה לרשת ובלי לגעת
ב-`data/`.

---

#### B2 · ולידציה של מזהים ב-routes

**הבעיה.** `meetingFilePath(id)` (`src/lib/store.ts:340-342`) משרשר את הפרמטר
מה-URL ישירות ל-`path.join`, ו-`DELETE /api/meetings/[id]` מגיע ל-`fs.rm`.
עבור העלאות כבר קיים `sanitizeOwnerId` (`store.ts:453`), אבל עבור מזהי פגישה
ופרסונה — לא.

גם אם Next.js מנרמל את הנתיב ברוב המקרים, אין סיבה להישען על התנהגות הראוטר
כשההגנה היא שורה אחת.

**מה לעשות.** הוסף ל-`src/app/api/_lib/schemas.ts` ולידטור
`idSchema = z.string().uuid()` והחל אותו בכל route שמקבל `[id]` או `[fileId]`:
meetings, personas, meeting-types, files, export, run. במקרה של כישלון — 400
עם הודעה בעברית ("מזהה לא תקין").

**קריטריון קבלה.** בקשה עם `id` שאינו UUID מחזירה 400 ולא נוגעת ב-filesystem.

---

#### B3 · ולידציית "מטרת הפגישה" בלקוח + תוויות שדות בעברית

**הבעיה.** שני חלקים:

1. `formValid = titleValid && typesValid && participantsValid`
   (`src/app/meetings/new/page.tsx:83`) — `objective` חסר. השרת כן דורש
   `min(1)` (`_lib/schemas.ts`), אז המשתמש מקבל שגיאה רק אחרי שליחה.
2. ההודעה שהוא מקבל היא: `השדה "objective" קטן מדי, קצר מדי, או ריק.` —
   `fieldLabel` (`src/app/api/_lib/http.ts:16`) מחזיר את ה-path הגולמי
   באנגלית. זה מפר את אינווריאנט 5.

**מה לעשות.**

1. הוסף `objectiveValid` ל-`formValid` והצג שגיאה inline מתחת לשדה, כמו שאר
   השדות.
2. הוסף ב-`http.ts` מפת תרגום `path → תווית עברית` (title→כותרת,
   objective→מטרת הפגישה, participantIds→משתתפים, meetingTypeIds→סוג פגישה,
   discussionRounds→מספר סבבים, name→שם, role→תפקיד, prompt→פרומפט...).
   כשאין תרגום — "הקלט".

**קריטריון קבלה.** שליחה בלי מטרה חסומה בלקוח עם הודעה בעברית; שגיאת שרת
מלאכותית מציגה תווית עברית ולא שם שדה באנגלית.

---

### קבוצה C — ערך למשתמש

---

#### C1 · הצגת עלות ושימוש בטוקנים

**הבעיה.** `usage` נאסף בקפדנות (`inputTokens`, `outputTokens`,
`cacheReadTokens`, `apiCalls`), נשמר על ה-Meeting, ומיוצא ב-JSON — ו**אף מסך
לא מציג אותו**. בכלי שמריץ Opus כמנחה עם effort גבוה, זה הנתון שהמשתמש הכי
צריך. זו התוספת עם יחס הערך/מאמץ הטוב ביותר בתוכנית.

**מה לעשות.**

1. הוסף `src/lib/pricing.ts` עם טבלת מחירים ל-1M טוקנים ופונקציית חישוב:

   | מודל | קלט $/1M | פלט $/1M |
   |---|---|---|
   | `claude-opus-5` | 5.00 | 25.00 |
   | `claude-sonnet-5` | 3.00 | 15.00 |
   | `claude-haiku-4-5` | 1.00 | 5.00 |

   קריאה מה-cache עולה ~0.1× ממחיר הקלט; כתיבה ל-cache ~1.25× (ב-TTL של 5
   דקות). כרגע נאסף רק `cacheReadTokens` — לחישוב מדויק כדאי להוסיף גם
   `cacheCreationTokens` (`usage.cache_creation_input_tokens`,
   `src/lib/anthropic.ts:159`).

   > הצג את התוצאה כ**הערכה**, לא כחיוב. המחירים בקוד הם snapshot ועשויים
   > להשתנות; ציין את זה ב-tooltip.

2. **מסך ההרצה** (`meetings/[id]/page.tsx`): שורת סטטוס חיה — מספר קריאות,
   טוקנים, עלות מוערכת. הנתונים מגיעים ב-`entry.usage` בכל אירוע SSE.
3. **רשימת הפגישות** (`meetings/page.tsx`): עמודת עלות. `usage` כבר חלק מ-
   `MeetingSummary` — אין צורך בשינוי API.
4. **ייצוא Markdown** (`export/render.ts`): סעיף "שימוש ועלות".

**קריטריון קבלה.** אחרי הרצה, העלות המוצגת סבירה מול `usage` הגולמי ב-JSON
המיוצא, ואחוז ה-cache reads גלוי (זה גם כלי האימות של A1).

---

#### C2 · חיווי "מי מדבר עכשיו"

**הבעיה.** תור של פרסונה על Sonnet/Opus הוא 20–60 שניות של מסך ללא שינוי. אין
שום חיווי מי צפוי לדבר.

**מה לעשות.** הרחב את פרוטוקול ה-SSE באירוע `{type:'speaking', speakerId,
speakerName, round}` שנשלח לפני כל קריאת מודל של פרסונה, והצג "בועה" בטעינה עם
השם והצבע. יש לעדכן במקביל: `src/lib/engine/types.ts` (`MeetingEvent`),
`src/lib/api-client.ts` (`RunEvent` + ה-switch), ו-`meetings/[id]/page.tsx`.

**שים לב:** מסלול ה-polling (במקרה של רענון דף) לא יקבל את האירוע הזה. זה בסדר
— החיווי הוא שיפור UX ולא מקור אמת.

---

#### C3 · שמירת תוצאות חיפוש הרשת, לא רק השאילתות

**הבעיה.** `extractResult` (`src/lib/anthropic.ts:149`) קולט רק את ה-`query`
מבלוקי `server_tool_use`. תוצאות החיפוש והציטוטים נזרקים. בכלי שכל התזה שלו
היא "אל תמציא" — אמירה שנשענת על חיפוש חייבת להיות ניתנת לאימות.

**מה לעשות.** קלוט גם בלוקי `web_search_tool_result` והרחב את
`TranscriptEntry.webSearches` לכלול כותרות ו-URLים. הצג מקורות בבועת התמליל
וב-Markdown המיוצא.

**שים לב לשגיאות:** כשל בכלי החיפוש חוזר כ-HTTP 200 עם בלוק
`web_search_tool_result` שה-`content` שלו הוא **אובייקט שגיאה** ולא מערך
תוצאות. יש לבדוק את המבנה לפני איטרציה.

---

#### C4 · הקשה מקלדת ונגישות בטפסים

**הבעיה.**

- כרטיס משתתף הוא `<button>` שמכיל `<input type="checkbox">`
  (`src/app/meetings/new/page.tsx:209-221`) — אלמנטים אינטראקטיביים מקוננים,
  HTML לא תקין והתנהגות מקלדת שבורה.
- אזור גרירת הקבצים הוא `div` עם `onClick` בלבד (שורה 240) — אין `tabIndex`,
  אין `onKeyDown`, אין `role`.

**מה לעשות.** החלף את כרטיס המשתתף ב-`<label>` שעוטף checkbox אמיתי (כל
הכרטיס נשאר לחיץ, בלי קינון כפתורים). לאזור הגרירה הוסף
`role="button" tabIndex={0}` ומטפל `onKeyDown` ל-Enter/Space.

---

### קבוצה D — לגיטימי לדחות

לא לבצע עכשיו; מתועד כדי שלא ייבחן מחדש בכל סבב.

- **`listMeetings` קורא כל תמליל** כדי לבנות סיכומים ואז זורק אותם
  (`src/lib/store.ts:352`). אחרי ~50 פגישות ארוכות עמוד הרשימה יאט. הפתרון —
  קובץ אינדקס — שווה לעשות כשזה יכאב בפועל.
- **מרוץ TOCTOU בהרצה כפולה:** שני טאבים על אותה פגישת `draft` יעברו שניהם את
  הבדיקה ב-`run/route.ts` ויריצו במקביל לאותו קובץ. נדיר בשימוש יחיד, אבל זה
  גם המקרה שבו החשבון מוכפל בשקט. אחרי A2 יהיה קל יותר לתקן (נעילה ב-store).
- **אין אימות משתמשים ואין הרשאות.** מכוון — הכלי מיועד להרצה מקומית.
- **הפריסה מוגבלת מבנית:** SSE שמחזיק חיבור 5–15 דקות + אחסון על הדיסק =
  לא ירוץ על Vercel/serverless. ראוי לתעד ב-README כ**החלטה** ולא רק כמגבלה.
- **אין זיכרון בין פגישות.** הצעד הזול הראשון בכיוון הזה הוא "יבוא תוצאות
  מפגישה קודמת כקובץ רקע" — לא נדרש RAG.
- **המשתמש לא יכול להתערב באמצע פגישה.** הפיצ'ר החסר הגדול ביותר ברמת המוצר,
  אבל הוא שינוי ארכיטקטוני ולא תיקון.

---

## 4. סדר עבודה מומלץ

```
B1  (בדיקות)          ← רשת ביטחון לכל מה שאחריו
 ├─ A1 (cache)         ← החיסכון הגדול; אימות דרך C1
 ├─ A4 (max_tokens)    ← קטן ועצמאי
 ├─ A2 (ביטול)
 └─ A3 (חילוץ מחדש)    ← נשען על אותו refactor של runner כמו A2
C1  (עלות)             ← גם הפיצ'ר וגם כלי המדידה של A1
B2, B3                 ← קטנים, אפשר לשלב בכל שלב
C2, C3, C4             ← לפי זמן
```

אם יש זמן למשימה אחת בלבד: **A1**. אם יש לשתיים: **A1 + C1** — יחד הן גם
מקטינות את העלות וגם הופכות אותה לגלויה.

---

## 5. הערות תפעוליות

- **ענף עבודה:** `claude/app-feedback-p143xq`. אין לדחוף לענף אחר.
- `npm run typecheck` חייב לעבור לפני כל commit. `next.config.ts` מגדיר
  `eslint.ignoreDuringBuilds: true`, כך ש-`npm run build` **לא** יתפוס בעיות
  lint — אל תסתמך עליו.
- `data/` אינו בגיט. מחיקת התיקייה מאתחלת מחדש את ה-seed בהרצה הבאה — דרך
  נוחה לאפס סביבת פיתוח.
- לבדיקות ידניות בלי לשרוף מפתח שרת: הזן מפתח אישי במסך `/settings`. הוא נשמר
  ב-`localStorage` ונשלח רק בכותרת `x-anthropic-api-key` בבקשת ההרצה.
- אין לוגים בצד שרת. בזמן עבודה על A1/C1 שווה להוסיף `console.log` זמני של
  `usage` בכל קריאה — ולהסיר לפני commit.
