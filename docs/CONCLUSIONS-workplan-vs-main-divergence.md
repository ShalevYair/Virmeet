# מסקנות: PR #12 (יישום docs/WORKPLAN.md) מול הארכיטקטורה הנוכחית ב-main

> נכתב אחרי שסבב ה-push האחרון של PR #12 חשף `mergeable_state: dirty` — לא
> מצב זמני, אלא התנגשות אמיתית ועמוקה. המסמך הזה בודק למה, ומציע מה לעשות
> עם זה. הוא לא מבצע שום מיזוג בעצמו.

---

## 1. מה קרה, בקצרה

שני ענפי עבודה יצאו מאותה נקודה (קומיט `d55f025`, "Allow a personal
Anthropic API key from the browser, not just server env") והתפצלו לשני
כיוונים בלתי-תואמים:

- **`claude/workplan-implementation-kqzdbz` (PR #12)** — יישם את
  `docs/WORKPLAN.md` (8 שלבים) על גבי הארכיטקטורה **הישנה**: אפליקציית
  Next.js עם שרת, `src/app/api/**`, אחסון בקבצי JSON על הדיסק (`data/`),
  ספק מודל יחיד (Anthropic).
- **`main`** — עבר שכתוב ארכיטקטוני מלא ל**אתר סטטי** שמתארח ב-GitHub
  Pages, לפי `docs/PLAN-static-github-pages.md` (מוזג ב-PR #2/#3/#4): כל
  ה-`src/app/api/**` **נמחק**, האחסון עבר מ-`fs` ל-**IndexedDB** בדפדפן,
  ונוסף **Gemini** כספק מודל שני לצד Anthropic. אחרי זה בוצע גם סבב תיקוני
  נכונות ותשתית הערכה נפרד, לפי `docs/PLAN-correctness-and-evaluation.md`
  (PR #5–#11).

התוצאה: `git diff` בין נקודת ההתפצלות ל-`main` מראה **78 קבצים, +8446/-3155
שורות**. כמעט כל קובץ ש-PR #12 נגע בו (`store.ts`, `types.ts`, `api-client.ts`,
`engine/runner.ts`, `engine/prompts.ts`, `engine/schemas.ts`, כל `src/app/api/**`)
זז, נכתב מחדש, או נמחק לגמרי ב-main. אין דרך סבירה למזג את PR #12 כמות
שהוא — הוא בנוי על הנחות אדריכליות (שרת, דיסק, ספק יחיד) שכבר לא נכונות.

**מסקנה 1:** זו לא תקלת מיזוג רגילה שפותרים עם `git rebase`. זו התנגשות בין
שתי תוכניות עבודה שונות שרצו על אותו ריפו בלי מודעות זו לזו.

---

## 2. עבודה שנעשתה פעמיים, באופן בלתי-תלוי

כמה מהבעיות ש-`docs/WORKPLAN.md` הצביע עליהן זוהו ותוקנו **גם** ב-main,
באופן עצמאי לגמרי, לפעמים כמעט באותה גישה:

| נושא | PR #12 (WORKPLAN) | main |
|---|---|---|
| ביטול פגישה שלא באמת מבטל | P1.1 — `AbortController` + בדיקת סטטוס | commit `f3fbe28`, "Fix cancel not actually cancelling a running meeting" |
| התאוששות מ-`running`/`failed` תקוע | P1.3 | commit `605de55` (#11), "Reset transcript and usage before re-running a cancelled or failed meeting" |
| תשתית בדיקות | P3.1 — vitest, 64 בדיקות | commit `8197f94` (#5), "Add vitest test infrastructure" — vitest, סט בדיקות נפרד |
| מדד נבדלות פרסונות (README §"איך לדעת אם הכלי עובד") | Stage 7 — `scripts/eval-distinctiveness.ts` | commit `b958a47` (#10), "Add the speaker-attribution eval" — `scripts/eval-attribution.ts` + `src/lib/eval/attribution.ts`, **אותו רעיון בדיוק**: להסתיר זהות דובר, לערבב, לבקש מהמודל לשייך בחזרה, למדוד מול קו בסיס |

זה סימן חשוב: שני הצדדים קראו את אותו README (או אותה בעיה) והגיעו לאותן
מסקנות, בלי לתאם. זה לא מפתיע ולא שגיאה של אף אחד — אבל זה אומר שכל מיזוג
עתידי חייב תחילה **השוואת קוד בפועל**, לא רק "לקחת את הגרסה של PR #12", כי
לפעמים הגרסה שכבר קיימת ב-main עדיפה או שקולה.

---

## 3. עבודה שקיימת רק ב-PR #12 (ולא הגיעה ל-main בשום צורה)

בדקתי ישירות מול `src/lib/types.ts` ו-`src/lib/eval/` ב-`main` — השדות/
הקבצים הבאים **אינם קיימים שם כלל**:

- **תקציב עלות ברמת הפגישה** (Stage 2 / P2.2) — `OrgSettings.maxMeetingApiCalls`
  / `maxMeetingTokens`, ובלימה בפועל של הדיון כשחוצים אותם. ב-`main`,
  `OrgSettings` לא כולל את השדות האלה בכלל.
- **שאלות המשך אחרי פגישה** (Stage 6) — `Meeting.followUps`, המסך הייעודי,
  והוצאתן לייצוא. אין שום דבר מקביל ב-main.
- **דיון מונחה-מנחה, ניסיוני** (Stage 8) — `Meeting.discussionMode`. אין
  ב-main.
- חלק מ-P1 (P1.2 timeout, P1.4 heartbeat) **לא רלוונטיים ב-main כמו שהם**:
  הם נבנו סביב זרימת SSE משרת (`POST /api/meetings/[id]/run` עם
  `ReadableStream`). ב-main **אין יותר SSE בכלל** — `runMeeting()` רץ ישירות
  בדפדפן ומדווח דרך callback רגיל (`meetings/view/page.tsx:401`,
  `runMeeting(id, {...})`), אז "heartbeat ל-SSE" הוא מושג שלא קיים שם. עדיין
  יכול להיות רלוונטי טיפול ב-timeout על קריאת המודל עצמה, אבל לא באותה צורה.

**מסקנה 2:** שלושת הפיצ'רים הממשיים (תקציב עלות, שאלות המשך, דיון
מונחה-מנחה) הם תוספת נטו על מה שקיים ב-main היום — לא כפילות, ולא משהו
שכבר נפתר אחרת. יש להם ערך גם בארכיטקטורה הסטטית, אבל **הם צריכים יישום
מחדש נגד הקוד הנוכחי**, לא cherry-pick — כי `runner.ts`, `types.ts`,
ו-`api-client.ts` שהם נשענים עליהם כבר לא קיימים באותה צורה.

---

## 4. עבודה שקיימת רק ב-main (ולא חלק מ-docs/WORKPLAN.md)

- שכתוב סטטי מלא ל-GitHub Pages, כולל `basePath`, ייצוא `manifest.json`
  אוטומטי, ותהליך פריסה ב-GitHub Actions.
- Gemini כספק מודל שני, כולל בחירת מנחה לפי איזה מפתח קיים
  (`pickFacilitatorModel`).
- אחסון IndexedDB במקום קבצי JSON, כולל התאמת `extract.ts` לעבודה על
  `File`/`Blob` בדפדפן במקום נתיב בדיסק.
- זיהוי חיתוך תשובה (`max_tokens` truncation) אצל שני הספקים (commit `c408e2c`).
- בדיקת מפתחות API מול המודלים שכל משתתף משתמש בהם **לפני** תחילת הפגישה
  (commit `0cc9b8d`).
- תיקון ל-Gemini סביב שילוב חיפוש-רשת ופלט מובנה (commit `0ecade4`).

זו עבודה ממשית ומתועדת (`docs/PLAN-static-github-pages.md`,
`docs/PLAN-correctness-and-evaluation.md`) שמוזגה דרך שרשרת PR מסודרת
(#2 עד #11) — לא נראית כמו טעות או push בשוגג.

---

## 5. נקודות לטיפול — דורשות החלטת בעלים, לא הנחת עבודה

1. **מה הארכיטקטורה הקנונית קדימה?** `main` היום הוא אתר סטטי, חי,
   ומתועד לעומק בשני מסמכי תכנון פנימיים. PR #12 בנוי על הארכיטקטורה
   הישנה עם שרת. אלה שני כיוונים סותרים לגמרי (יש שרת / אין שרת, ספק אחד /
   שני ספקים, דיסק / IndexedDB) — אי אפשר "לשלב" אותם חלקית בלי להחליט
   קודם איזה מהם המשך הנכון. **בלי החלטה מפורשת כאן, אסור להמשיך לבצע
   מיזוג — זה בדיוק סוג הניחוש שהתוכניות הפנימיות (`PLAN-correctness-
   and-evaluation.md §0`) מזהירות מפניו במפורש.**

2. **מה קורה ל-PR #12?** בהינתן ש-`main` הוא כנראה הכיוון הפעיל (יש לו
   אתר חי, GitHub Actions, ותיעוד תכנון עדכני) — הסבירות הגבוהה היא ש-PR #12
   **לא ניתן למיזוג כמות שהוא**. שתי אפשרויות מעשיות:
   - **א.** לסגור את PR #12 בלי מיזוג, ולהפיק ממנו רק את שלוש התוספות
     בסעיף 3 (תקציב עלות, שאלות המשך, דיון מונחה-מנחה) כ-PR-ים חדשים,
     קטנים, נגד `main` הנוכחי.
   - **ב.** לבצע מיזוג ידני מלא — יקר וממושך, ורוב הסיכוי שרוב הקוד של
     PR #12 יזרק בדרך כי הוא נכתב נגד שכבת אחסון/API שכבר לא קיימת.
   ההמלצה שלי: **אפשרות א'**, כי היא היחידה שלא דורשת לזרוק את עבודת השכתוב
   הסטטי המתועדת שכבר מוזגה.

3. **`docs/WORKPLAN.md` עצמו טעון עדכון.** הוא נכתב (או לפחות בוצע) בלי
   מודעות לכך ש-`docs/PLAN-static-github-pages.md` כבר מוזג ב-main. אם
   הכיוון הוא להמשיך לפעול לפי `docs/WORKPLAN.md`, השלבים שנותרו (וגם
   ניסוח מחדש של מה שכבר בוצע) צריכים להיכתב מול הארכיטקטורה הסטטית
   הנוכחית — כולל, למשל, לוודא ש"heartbeat ל-SSE" מוחלף במושג שרלוונטי
   בפועל לזרימה בדפדפן.

4. **אם שלוש התוספות (סעיף 3) יעברו יישום מחדש**, כדאי סדר עדיפויות:
   תקציב עלות ושאלות המשך הם שינויים מוכלים יחסית (שדות על `OrgSettings`/
   `Meeting`, פונקציה אחת חדשה שקוראת ל-`callModel` הקיים). דיון
   מונחה-מנחה משנה בפועל את לולאת הדיון ב-`runner.ts` — כדאי לבצע אותו
   אחרון ורק אחרי שקיים מדד (`eval-attribution.ts`, שכבר קיים ב-main) כדי
   שאפשר יהיה למדוד את ההשפעה בפועל, בדיוק כפי שדרשה גם התוכנית המקורית.

5. **לתעד את הפער הזה כדי שלא יקרה שוב.** מומלץ שכל תוכנית עבודה עתידית
   (בין אם `docs/WORKPLAN.md` המשך, ובין אם `docs/PLAN-*.md` חדש) תפתח
   בבדיקה מפורשת: "האם `main` השתנה מהותית מאז שהמסמך הזה נכתב?" — בדיוק
   כמו שכבר עושה `docs/PLAN-correctness-and-evaluation.md` בשורה הראשונה
   שלו ("אומת מול `89d507b`").

---

_Generated by [Claude Code](https://claude.ai/code)_
